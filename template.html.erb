<!DOCTYPE html>
<html>
  <head>
    <title>Untitled</title>
    <!-- Latest compiled and minified CSS -->
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.2/css/bootstrap.min.css">
  </head>
  <body>
    <div class="container">
      <div class="text-center"><h1> Ebay Category tree for categoryParentId = <%= @categories.first[0] %></h1></div>
      <table class="table table-hover" style="margin-top: 3em;">
        <thead>
          <tr>
            <th>CategoryID</th>
            <th>CategoryName</th>
            <th>CategoryLevel</th>
            <th>BestOfferEnabled</th>
          </tr>
        </thead>
        <% @categories.each do |category| %>
          <tbody>
            <td> <%= category[0] %> </td>
            <td> <%= category[2] %> </td>
            <td> <%= category[1] %> </td>
            <td> <%= category[5] %> </td>
          </tbody>
        <%end %>
      </table>
    </div>
  </body>
</html>